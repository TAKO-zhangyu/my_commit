<table>
  <thead>
    <tr>
      <th>State</th>
      <th>Limit</th>
      <th>Task</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @tasks.each do |t| %>
    <tr>
      <td><%= t.state %></td>
      <td><%= t.limit_date %></td>
      <td><%= t.task %></td>
      <td>
        <a href="/tasks/<%= t.id %>">edit</a>
      </td>
      <td>
        <button type="submit">delete</button>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

<% flash.each do |msg| %>
  <p><%= msg %></p>
<% end %>

<table>...</table>